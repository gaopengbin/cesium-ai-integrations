# ========================================
# Geolocation Server Configuration
# ========================================

# Google Maps API Key (required for Google providers)
# Get your key from: https://console.cloud.google.com/
# Required APIs: Places API (New), Routes API
GOOGLE_MAPS_API_KEY=your_api_key_here

# Provider Configuration
# Places Provider: "nominatim" (default, free geocoding), "overpass" (free POI search), or "google" (paid, full features)
PLACES_PROVIDER=nominatim

# Routes Provider: "osrm" (default, free, no API key) or "google" (requires API key)
ROUTES_PROVIDER=osrm

# OpenStreetMap User-Agent (recommended for Nominatim and Overpass)
# Should include app name/version and contact information
# OSM_USER_AGENT="MyApp/1.0 (contact@example.com)"

# Overpass Server URL (only used when PLACES_PROVIDER=overpass)
# Default: https://overpass-api.de/api/interpreter (public instance)
# For production, consider self-hosting: https://github.com/drolbr/Overpass-API
# OVERPASS_SERVER_URL=https://overpass-api.de/api/interpreter

# OSRM Server URL (only used when ROUTES_PROVIDER=osrm)
# Default: https://router.project-osrm.org
# OSRM_SERVER_URL=https://router.project-osrm.org

# Server Configuration
# No additional server port configuration required

# User-Agent for OSM requests (required by OSM usage policy: https://operations.osmfoundation.org/policies/nominatim/)
# OSM_USER_AGENT=